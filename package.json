{
  "name": "esg-datavault-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for ESG DataVault using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:comprehensive": "playwright test final-comprehensive-workflow",
    "test:core": "playwright test core-workflow-validation",
    "test:explore": "playwright test ui-exploration-test",
    "show-report": "playwright show-report",
    "mcp:cleanup": "pkill -f 'chrome.*--remote-debugging-port' 2>/dev/null || true; pkill -f 'chromium.*--remote-debugging-port' 2>/dev/null || true; pkill -f 'npx @playwright/mcp' 2>/dev/null || true; rm -rf ~/.cache/ms-playwright/mcp-chrome-* 2>/dev/null || true; rm -rf ~/Library/Caches/ms-playwright/mcp-chrome-* 2>/dev/null || true",
    "mcp:start": "npm run mcp:cleanup && sleep 1 && npx @playwright/mcp --port 3001 --headless --isolated --viewport-size '1280,720'",
    "mcp:start-headed": "npm run mcp:cleanup && sleep 1 && npx @playwright/mcp --port 3001 --viewport-size '1280,720'",
    "mcp:restart": "npm run mcp:cleanup && sleep 2 && npm run mcp:start"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.36",
    "@playwright/test": "^1.53.1"
  }
}
